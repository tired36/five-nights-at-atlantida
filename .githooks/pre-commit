#!/bin/sh
# Bloquea commits si detecta URIs de MongoDB con contraseña real.
cd "$(git rev-parse --show-toplevel)" || exit 1
npm run check-secrets -- --staged 2>/dev/null || node scripts/check-secrets.js --staged
exit $?
